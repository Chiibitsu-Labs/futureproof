# Google Sheet webhook for founding-cohort applications.
# Apps Script web-app /exec URL. See api/apply.js for setup steps.
SHEET_WEBHOOK_URL=

# Optional: where the "apply / DM" buttons point if you'd rather route to a
# DM or external form instead of the on-page form.
# Leave empty to use the built-in on-page application form.
# [FLAG ~ CTA DESTINATION: set this before launch, or keep the on-page form.]
VITE_APPLY_URL=
